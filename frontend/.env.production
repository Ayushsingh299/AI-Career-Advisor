# Production build configuration
# Disable ESLint plugin during production builds to avoid warnings being treated as errors
DISABLE_ESLINT_PLUGIN=true
TSC_COMPILE_ON_ERROR=true

# Allow build to continue even with minor linting issues
SKIP_PREFLIGHT_CHECK=true